[project]
name = "ol-lab-benchmark"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy",
    "pandas",
    "datasets",
    "rank-bm25",
    "requests>=2.32.4",
    "sentence-transformers",
]
